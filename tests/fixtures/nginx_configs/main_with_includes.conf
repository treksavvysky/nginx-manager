# Test configuration with include directives

server {
    listen 80;
    server_name main.example.com;
    root /var/www/main;

    include included_locations.conf;
}

upstream main_backend {
    server 127.0.0.1:8000;
}
